package com.psildave.punchtheclock.ui.theme

import androidx.compose.foundation.shape.CircleShape
import androidx.compose.foundation.shape.RoundedCornerShape

/**
 * Main circular shape used for the primary punch action button.
 */
val WearShapePunchButton = CircleShape

/**
 * Fully rounded shape used for chips and small navigation buttons.
 */
val WearShapeChip = RoundedCornerShape(percent = 50)

/**
 * Moderately rounded shape used for cards and informational containers.
 */
val WearShapeCard = RoundedCornerShape(percent = 20)

/**
 * Circular shape used for secondary action icons.
 */
val WearShapeIconButton = RoundedCornerShape(percent = 50)
